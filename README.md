## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install vite`
2. Create the `GEMINI_API_KEY` from google gemini then create [.env.local](.env.local) file and paste your Gemini API key (optional - for better experience)
3. Run the app:
   `npm run dev`
